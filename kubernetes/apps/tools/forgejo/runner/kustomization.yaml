---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./ocirepository.yaml
  - ./helmrelease.yaml
  - ./externalsecret.yaml
  - ./netpol.yaml
  - ./priorityclass.yaml

configMapGenerator:
  - name: forgejo-runner-values
    files:
      - config.yaml
      - podspec-default.yaml=podspecs/default.yaml
      - podspec-dind.yaml=podspecs/dind.yaml

generatorOptions:
  disableNameSuffixHash: true
