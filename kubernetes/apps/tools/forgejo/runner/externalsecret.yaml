---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: forgejo-runner-secret
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect

  target:
    template:
      data:
        uuid: '{{ .forgejo_runner_instance_uuid }}'
        token: '{{ .forgejo_runner_instance_token }}'

  dataFrom:
    - extract:
        key: forgejo
