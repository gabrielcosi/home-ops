---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: freeze-frame
spec:
  chartRef:
    kind: OCIRepository
    name: freeze-frame
  interval: 1h
  values:
    controllers:
      freeze-frame:
        pod:
          imagePullSecrets:
            - name: registry-credentials
          securityContext:
            runAsNonRoot: true
            runAsUser: 65534
            runAsGroup: 65534
            fsGroup: 65534
            fsGroupChangePolicy: OnRootMismatch
            seccompProfile:
              type: RuntimeDefault

        containers:
          app:
            image:
              repository: reg.xcd.dev/private/freeze-frame
              tag: latest@sha256:e78231d213f71ecdf9467e49e2f06de95a363e5f3a88df870c43e5985ecacb28
            resources:
              requests:
                cpu: 1m
                memory: 10Mi
              limits:
                memory: 10Mi
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop: ['ALL']

    persistence:
      tmp:
        type: emptyDir
        globalMounts:
          - path: /var/cache/nginx
          - path: /var/run
          - path: /tmp

    service:
      app:
        ports:
          http:
            port: 8080

    route:
      app:
        hostnames:
          - 'freeze-frame.xcd.dev'
        parentRefs:
          - name: envoy-internal
            namespace: network
