---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./ocirepository.yaml
  - ./externalsecret.yaml
  - ./prometheusrule.yaml
  - ./helmrelease.yaml
configMapGenerator:
  - name: gatus-config
    files:
      - config.yaml=./resources/config.yaml
      - buddy.yaml=./resources/buddy.yaml
generatorOptions:
  disableNameSuffixHash: true
  annotations:
    kustomize.toolkit.fluxcd.io/substitute: disabled
