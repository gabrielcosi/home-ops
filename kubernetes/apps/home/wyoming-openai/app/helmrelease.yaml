---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: wyoming-openai
spec:
  chartRef:
    kind: OCIRepository
    name: wyoming-openai
  interval: 1h
  values:
    controllers:
      wyoming-openai:
        annotations:
          reloader.stakater.com/auto: 'true'
        pod:
          securityContext:
            runAsNonRoot: true
            runAsUser: 65534
            runAsGroup: 65534
            fsGroup: 65534
            fsGroupChangePolicy: OnRootMismatch
            seccompProfile:
              type: RuntimeDefault
        containers:
          app:
            image:
              repository: ghcr.io/roryeckel/wyoming_openai
              tag: 0.6.0
            env:
              WYOMING_URI: 'tcp://0.0.0.0:10300'
              WYOMING_LOG_LEVEL: INFO
              WYOMING_LANGUAGES: en
              STT_OPENAI_URL: 'http://bifrost.ai.svc.cluster.local:8080/v1'
              STT_MODELS: zeus/stt
              STT_BACKEND: OPENAI
              TTS_OPENAI_URL: 'http://bifrost.ai.svc.cluster.local:8080/v1'
              TTS_MODELS: zeus/tts
              TTS_VOICES: angel.wav
              TTS_BACKEND: OPENAI
            envFrom:
              - secretRef:
                  name: wyoming-openai-secrets
            probes:
              liveness:
                enabled: true
                custom: true
                spec:
                  tcpSocket:
                    port: 10300
                  initialDelaySeconds: 5
                  periodSeconds: 10
                  timeoutSeconds: 1
                  failureThreshold: 3
              readiness:
                enabled: true
                custom: true
                spec:
                  tcpSocket:
                    port: 10300
                  initialDelaySeconds: 3
                  periodSeconds: 10
                  timeoutSeconds: 1
                  failureThreshold: 3
              startup:
                enabled: true
                custom: true
                spec:
                  tcpSocket:
                    port: 10300
                  periodSeconds: 5
                  failureThreshold: 30
                  timeoutSeconds: 1
            resources:
              requests:
                cpu: 25m
                memory: 256Mi
              limits:
                memory: 256Mi
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                  - ALL

    service:
      wyoming-openai:
        ports:
          wyoming:
            port: 10300
