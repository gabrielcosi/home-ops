---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: ${APP}-anubis
spec:
  commonMetadata:
    labels:
      app.kubernetes.io/name: &app ${APP}
  interval: 1h
  path: ./kubernetes/components/anubis/base/anubis
  postBuild:
    substitute:
      APP: *app
      APP_NAMESPACE: ${APP_NAMESPACE}
      ANUBIS_TARGET: ${ANUBIS_TARGET}
      ANUBIS_DIFFICULTY: '${ANUBIS_DIFFICULTY:="4"}'
      ANUBIS_ALLOW_PATH_REGEX: '${ANUBIS_ALLOW_PATH_REGEX:=^/-anubis-extra-paths-disabled-}'
      ANUBIS_VALKEY_URL: '${ANUBIS_VALKEY_URL:=redis://dragonfly.database.svc.cluster.local:6379/10}'
  prune: true
  retryInterval: 2m
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: ${APP_NAMESPACE}
  timeout: 5m
  wait: false
